const create = require('./create');
const login = require('./login');
const getAll = require('./getAll');
const getById = require('./getById');
const exclude = require('./exclude');

module.exports = { create, exclude, getAll, getById, login };
